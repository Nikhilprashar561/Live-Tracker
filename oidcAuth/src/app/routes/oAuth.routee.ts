import express from 'express';

const router = express.Router();

// router.route('/register/application').post();
// router.route('/client/credentials').post(); 

export { router as oAuthRouter };


// Need one Endpoint to register the client application.
// Need one Endpoint to get Appliction Data register by application
